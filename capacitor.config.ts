import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dashboard.financial',
  appName: 'Financial Dashboard',
  webDir: 'out'
};

export default config;
